import os
import sys
sys.path.insert(1, os.path.join(sys.path[0], '..'))

from queries.core import create_table, insert_data, get_data, delete_data

# create_table()
# insert_data()
# get_data()
delete_data()